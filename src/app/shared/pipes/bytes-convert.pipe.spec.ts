import { BytesConvertPipe } from './bytes-convert.pipe';

describe('BytesConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new BytesConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
