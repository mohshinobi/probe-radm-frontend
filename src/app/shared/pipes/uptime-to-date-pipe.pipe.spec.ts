import { UptimeToDatePipePipe } from './uptime-to-date-pipe.pipe';

describe('UptimeToDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UptimeToDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
